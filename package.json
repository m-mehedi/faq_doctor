{
  "name": "smart-nurse-faq-capture",
  "version": "1.1.0",
  "description": "smart-nurse-faq-captures-basically-records-questions-asked-by-the-doctor-or-nurse",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip-package": "cd .. && rm -f package.zip && zip -r package.zip *",
    "zip-lambda": "rm -f lambda.zip && zip -r lambda.zip *"
  },
  "author": "Amazon Alexa",
  "license": "ASL",
  "dependencies": {
    "ask-sdk-core": "^2.6.0",
    "ask-sdk-model": "^1.18.0",
    "airtable": "^0.11.6",
    "dotenv": "^16.0.3",
    "lodash": "4.17.21"
  }
}
